# Week 3 review

45 scikit-learn task runs passed; 5 XGBoost-only tasks skipped, not tested. Training-only CV and validation-only threshold selection preserve the separate final test. Added None-to-NaN normalization before categorical imputation, with a regression test. XGBoost full-project mode remains unverified because the package is absent.

